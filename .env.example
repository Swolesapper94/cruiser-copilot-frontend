# The backend this frontend talks to. Defaults to the backend's own default
# port, so the two repos work together with no configuration out of the box.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
